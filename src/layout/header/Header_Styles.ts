import styled from 'styled-components';

const Header = styled.header`
  background-color: var(--bg-color-dark);
  padding: 20px 0;
  max-height: 70px;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  opacity: 0.95;
  z-index: 9999;
  
`
export const S = {
    Header
}